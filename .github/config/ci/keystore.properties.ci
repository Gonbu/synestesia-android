# Configuration keystore CI/CD - Valeurs factices pour le build
# Ce fichier est utilisé uniquement en CI, pas en développement

# Keystore configuration (valeurs factices pour le build)
KEYSTORE_PASSWORD=ci_fake_keystore_password
KEY_ALIAS=ci_fake_release
KEY_PASSWORD=ci_fake_key_password

# Ces valeurs ne permettent pas de signer les APK
# Elles servent uniquement à faire passer la compilation
